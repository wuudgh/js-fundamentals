// do not edit these lines
const numOne = 8;
const numTwo = 16;
const numThree = 32;

// TODO: Add code below using Javascript numeric operators so that the tests pass

// Set this variable to numOne added to numTwo
// To set the variable to numOne added to numTwo, all i have to do is add a plus sign between the two variables.
const numOnePlusNumTwo = numOne + numTwo;

// Set this variable to numThree multiplied by numTwo
//To set the varaible to numThree multiplied by numTwo.
// I will will add a multiplication sign between the two varaible numThree and numTwo
const numThreeTimesNumTwo = numThree * numTwo;

// Set this variable to numThree divided by numOne
//To set the varaible to numThree devided by numOne.
// I will put a devision sign between the two varaibles.
const numThreeDividedByNumOne = numThree / numOne;

// Set this variable to numThree minus numOne
//To set the variable to numThree minus numOne.
// I will put the minus sign between the two variables.
const numThreeMinusNumOne = numThree - numOne;

// Set this variable to the sum of numOne, numTwo and numThree
//To this varaible to the sum of numOne, numTwo and numThree.
// i will just keep putting plus sign between the variables
const sum = numOne + numTwo + numThree;

// Set this variable to the sum of (numOne, numTwo, numThree) divided by numOne
//To set this variable to the sum of (numOne, numTwo, numThre) divided by numOne
// I will  put plus sing between them in to brackets  then a add the division sign between numOne and the bracket.
const numBytes = (numOne + numTwo + numThree) / numOne;

// do not edit the exported object.
module.exports = {
  a: numOnePlusNumTwo,
  b: numThreeTimesNumTwo,
  c: numThreeDividedByNumOne,
  d: numThreeMinusNumOne,
  e: sum,
  f: numBytes,
};
